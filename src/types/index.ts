/**
 * Types partagés de l'application Streek.
 *
 * Placeholder pour la phase fondations. Les types métier (habitudes, tâches,
 * objectifs) et les types générés depuis le schéma Supabase (`Database`)
 * viendront s'ajouter ici dans les phases suivantes.
 */

export {};
